query ($id: ID!){
  getPublisher (id:$id) {
    id
    name
    date
    url
    api {
      widgetUrl
      token
    }
    contact {
      id
      firstName
      lastName
      email
      phone
    }
  }
}
