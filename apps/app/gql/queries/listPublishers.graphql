query ($skip: Int){
  listPublishers (skip: $skip, limit: 500){
    id
    name
    date
    api {
      widgetUrl
      token
    }
    contact {
      id
      firstName
      lastName
      email
      phone
    }
  }
}
